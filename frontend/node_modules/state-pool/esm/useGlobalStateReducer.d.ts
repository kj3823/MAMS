import { GlobalState } from './GlobalState';
declare type Reducer = (state: any, action: any) => any;
declare type Config = {
    selector?: (state: any) => any;
    patcher?: (state: any, selectedStateValue: any) => any;
};
declare type ReturnType<T> = [
    state: T,
    dispatch: (action: any) => any
];
declare function useGlobalStateReducer<T = any>(reducer: Reducer, globalState: GlobalState<any>, config?: Config): ReturnType<T>;
export { useGlobalStateReducer };
